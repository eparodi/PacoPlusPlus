-Change Types Table from HashTable to Dynamic Vector.

How to start the compiler types.

TASKS                               | FILE
------------------------------------|-------------------------------------------
1) startTypes();                    | types/types.c
2) Create all types:                |
  createType(char *, size_t size);  | types/types.c
3) buildOpTable();                  | types/types.c
4) Load the operation table.        | Soon.